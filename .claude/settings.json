{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit|MultiEdit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_response.filePath // .tool_input.file_path' | { read -r f; case \"$f\" in *.cpp|*.hpp|*.h|*.cc|*.cxx|*.hxx|*.c) clang-format -i --style=file \"$f\";; esac; } 2>/dev/null || true"
          }
        ]
      }
    ]
  }
}
