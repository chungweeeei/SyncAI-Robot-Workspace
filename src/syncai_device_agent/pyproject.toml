[project]
name = "syncai-device-agent"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "deepagents>=0.7.5",
    "dotenv>=0.9.9",
    "requests>=2.34.2",
    "structlog>=26.1.0",
]

[project.scripts]
syncai-device-agent = "syncai_device_agent:main"

[build-system]
requires = ["uv_build>=0.11.28,<0.12.0"]
build-backend = "uv_build"
