# colcon / ROS2 build artifacts
build/
install/
log/
.colcon/


# rosdep / ament
*.deb
.catkin_tools/
devel/

# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
.eggs/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.venv/
venv/

# C/C++
*.o
*.a
*.so
*.so.*
*.dylib
*.dSYM/

# CMake
CMakeCache.txt
CMakeFiles/
cmake_install.cmake
Makefile

# Editor / IDE
.vscode/*
!.vscode/settings.json
!.vscode/extensions.json
!.vscode/launch.json
!.vscode/tasks.json
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Logs / temp
*.log
*.bag
*.mcap
*.tmp


# environment variables
.env

# docker bind-mount data (postgres / pgadmin)
data/

# test repo
src/syncai_test_node

# Stale standalone clone of the MCP server, kept only as a local scratch copy.
# The package now lives vendored in-tree at src/syncai_ros_mcp (same project, same
# git lineage — this copy's HEAD 599526f is an ancestor of the vendored 828cf71).
# Two working copies of one ament package would give colcon a duplicate
# `syncai_ros_mcp`, so the directory also carries a COLCON_IGNORE marker.
src/syncai-ros-mcp/

# LIO map output (pcd / gridmap / poses — regenerated, not tracked).
# Anchored with a leading slash: a bare `map/` matches a directory of that name at
# any depth, and it was silently swallowing
# src/syncai_backend/syncai_backend/repositories/map/. Files already tracked there
# stayed visible, so nothing looked wrong until a new module in it went missing
# from a commit.
/map/

record/